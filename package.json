{
  "name": "web-ugc-generator-prompt",
  "version": "1.0.0",
  "private": true,
  "description": "Sinergi Visual UGC Generator Prompt",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  }
}
